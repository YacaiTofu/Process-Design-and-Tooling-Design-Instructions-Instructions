#import "../definitions.typ": indent, ph


= 工艺规程设计
== 确定毛坯制造形式
#indent 零件材料为 HT200 灰铸铁，重量 1.4kg。考虑到其为大批大量生产，毛坯成形方法应当采用机器造型砂型铸造，不使用手工木模，而采用金属模机器造型砂型铸造。金属模精度高、寿命长，铸件尺寸精度高、表面质量好，适合大批量重复生产。\
#indent 铸件仅铸出 $#sym.phi.alt 100$ 刻度盘的棒料毛坯，中心 $#sym.phi.alt 20$ 通孔、$4 times #sym.phi.alt 9$ 通孔、侧面 $#sym.phi.alt 9$ 小孔等均不铸出，全部留给机加工完成，避免铸造孔产生错箱、气孔、粘砂等缺陷造成废品。\
#indent 零件上的 R5 过渡圆角、$3 times 2$ 退刀槽、各类倒角均不铸出，全部依靠切削加工获得。\
#indent 铸造完成后毛坯统一进行去应力退火，消除铸造残余内应力，防止机加工之后零件变形，保证使用过程尺寸稳定。同时铸件不允许裂纹、气孔、砂眼等有害缺陷；设置浇口、冒口，铸件完成后清理飞边毛刺；大批量可通过流水线进行落砂、清理、退火。
== 基准选择
#indent 基准选择是工艺规程设计中的重要工作之一。基准选择的正确与合理，可以使加工质量得到保证，生产率得以提高，否则，加工工艺过程中会问题百出，更有甚者还会造成零件大量报废，使生产无法正常进行。
=== 粗基准的选择
#indent 对于一般轴类、法兰类铸件零件，选择粗基准时应遵循四条基本原则：保证不加工表面与加工表面的位置关系、合理分配各表面加工余量、粗基准一般不得重复使用、应选择平整光洁且无飞边毛刺的毛坯表面作为粗基准。\
#indent 本法兰盘零件为HT200灰铸铁机器造型砂型铸造毛坯，零件中心$#sym.phi.alt 20$通孔及各小孔均未铸出，不存在可供定位的毛坯内孔。综合以上条件，选择毛坯$#sym.phi.alt 100$外圆铸造表面与毛坯B端面作为粗基准，采用三爪自定心卡盘夹持毛坯$#sym.phi.alt 100$外圆，以毛坯B端面实现轴向定位。\
#indent 选用该组粗基准，能够保证铸件不加工外轮廓与内部各加工表面之间壁厚均匀，避免出现局部壁厚过薄的缺陷；同时可以合理分配$#sym.phi.alt 100$外圆、B端面等重要工作表面的加工余量。金属模铸造得到的毛坯$#sym.phi.alt 100$外圆与B端面较为平整，无明显飞边毛刺，适合三爪卡盘装夹定位，适配大批大量生产的装夹要求。\
#indent 粗基准仅在首道粗车工序中使用，完成粗车加工之后，毛坯铸造表面已被切削去除，后续全部工序不再复用该粗基准，严格遵守粗基准只使用一次的原则。\
=== 精基准的选择
#indent 粗加工完成后需要选择精基准，精基准选择主要遵循基准重合原则、基准统一原则、自为基准原则、互为基准原则。本零件图纸规定$#sym.phi.alt 20^(+0.045)_0$通孔为基准A，零件多项形位公差（径向圆跳动、端面圆跳动）均以该内孔为设计基准。\
#indent 大批大量生产，优先采用基准重合原则，以加工完成的$#sym.phi.alt 20$通孔作为主要精基准。加工外圆、端面等回转表面时，采用可胀心轴（涨套心轴）装入$#sym.phi.alt 20$孔内定位，限制零件五个自由度，实现设计基准与工艺基准重合，消除基准不重合误差，保证$#sym.phi.alt 100$外圆、B端面对基准A的跳动精度要求。\
#indent 在车削加工部分工序中，也可采用已经精加工后的$#sym.phi.alt 100$外圆作为辅助精基准，三爪卡盘装夹外圆，用来镗削$#sym.phi.alt 20$通孔，属于互为基准：以外圆定位加工内孔，再以内孔心轴定位加工外圆与端面，反复提高内外圆之间的位置精度。
#indent 铣削、钻床工序加工平面与各小孔时，使用V型块托住已加工$#sym.phi.alt 45$或者$#sym.phi.alt 100$外圆作为精基准，保证孔、平面对零件回转中心的位置尺寸。\
#indent 本零件尽量做到基准统一，多数工序优先使用$#sym.phi.alt 20$孔心轴定位，减少基准变换带来的定位误差，简化大批大量生产的夹具结构，提升生产效率。
== 制定工艺路线
#indent 制定工艺路线的出发点，应当是使零件的几何形状、尺寸精度及位置精度等级要求得到合理的保证。在生产纲领已经确定为大批大量生产的条件下，可以考虑采用万能机床并配以专用工夹具，尽量使工序集中，以提高生产率。除此以外，还应当考虑经济效果，以便使生产成本尽量下降。\
=== 工艺路线方案一
#grid(
  columns: 2,
  column-gutter: 4em,
  row-gutter: 1em,
  [#h(4em)工序1 ], [粗车B面、右侧柱体、左右端面、右端面倒角及3×2退刀槽],
  [#h(4em)工序2], [钻$ph 20$通孔],
  [#h(4em)工序3], [钻$4 times ph 9$透孔],
  [#h(4em)工序4], [钻$ph 4$孔并扩$ph 6$孔],
  [#h(4em)工序5], [倒C1.5、C1倒角与R5两处圆角],
  [#h(4em)工序6], [铣B面两侧柱体两端面（1.6）控制两端面相对于A基准跳动误差0.03],
  [#h(4em)工序7], [磨B面两边柱体两侧面，右侧柱体侧面（0.8）],
  [#h(4em)工序8], [磨B面],
  [#h(4em)工序9], [抛光B面（0.4）],
  [#h(4em)工序10], [刻线刻字],
)
=== 工艺路线方案二
#grid(
  columns: 2,
  column-gutter: 4em,
  row-gutter: 1em,
  [#h(4em)工序1 ], [粗车全部，形成整体轮廓],
  [#h(4em)工序2], [$ph 20$孔，钻孔并扩孔],
  [#h(4em)工序3], [以右端面为基准，半精车左半部分],
  [#h(4em)工序4], [以$ph 100$圆柱面中心线为基准，半精车右半部分，并铰孔],

  [#h(4em)工序5], [精车$ph 100$mm端面、外圆，精车B面，车过渡圆角R5],
  [#h(4em)工序6], [粗铣、精铣$ph 90$mm柱体的两侧平面],
  [#h(4em)工序7], [钻$ph 4$mm底孔，扩$ph 6$mm孔],
  [#h(4em)工序8], [钻$4 times ph 9$mm透孔],
  [#h(4em)工序9], [粗磨全部有粗糙度需求的面，粗糙度达到Ra3.2],
  [#h(4em)工序10], [分曲面和平面精磨所有粗糙度要求较高的面，精磨至Ra1.6、Ra0.8],
  [#h(4em)工序11], [去除各孔口、端面、沟槽、铣削平面处加工毛刺飞边，清理零件尖角铁屑],
  [#h(4em)工序12], [B端面抛光处理，以及所有粗糙度要求为Ra0.4的面，并使其粗糙度达到Ra0.4],
  [#h(4em)工序13], [$ph 100$mm外圆表面刻字、刻线],
  [#h(4em)工序14], [$ph 100$mm外圆表面镀铬],
)
=== 工艺方案的比较与分析
#indent 上述两个工艺方案的特点在于：\
#indent 方案一加工顺序基本遵循先粗后精的加工思路，先完成车削成型，集中进行孔加工，经铣削、磨削后采用抛光实现 B 面 Ra0.4 光整要求，并对铣削工序的形位公差提出约束。但该方案仍存在较多缺陷：未区分粗车、半精车、精车，粗精加工混杂；$ph 20$ 孔仅钻孔，缺少扩铰工序，基准孔精度不足；钻床孔加工拆分为多道工序，重复装夹；倒角圆角单独设置工序，工序划分不合理；磨削未设置粗磨工序，磨削余量分配不合理；无去毛刺工序，大批量生产工艺稳定性较差。\
#indent 方案二整体遵循先粗后精、先主后次的加工原则，回转表面加工划分为粗车、半精车、精车阶段，有利于分散切削负荷，减小精加工阶段的加工变形。基准孔$ph 20$采用钻孔‑扩孔‑铰孔完整加工流程，能够保证基准孔的尺寸精度与表面质量，为后续工序提供可靠定位基础。\
#indent 铣削加工安排在主要回转面车削完成之后，磨削分为粗磨、精磨两级加工：粗磨去除大部分加工余量，将表面加工至 Ra3.2；精磨进一步保证尺寸与形位公差，表面达到 Ra0.8；再通过抛光工序将高要求表面光整至 Ra0.4，光整加工逻辑合理。\
#indent 工序中设置独立去毛刺工序，放在磨削之后、抛光之前，可以清除车、钻、铣加工产生的飞边毛刺，避免毛刺损伤光整表面，改善抛光与后续表面处理的加工条件。整体工艺流向清晰，主要成型加工在前，光整、标记、表面处理工序置于末尾。\
#indent 但方案二也存在一些不足：工序基准表述不严谨，车床铰孔不利于保证孔径精度；钻床孔加工分为两道工序，存在重复装夹；磨削工序未区分平面磨与外圆磨设备，工序描述笼统；刻字置于镀铬之前，镀层会覆盖刻字；不同粗糙度表面需要依靠工序内划分工步调整磨削参数实现。\
#indent 综合两套初步方案的优缺点，得到本工艺路线。车削划分为粗车‑半精车‑精车，合理分配加工余量；$ph 20$基准孔采用钻‑扩‑铰完整工艺，铰孔安排在 Z525 立式钻床上完成，保证孔径精度。所有孔加工集中在一道钻床工序内通过工步实现，避免工件多次装夹找正。磨削区分粗磨、精磨阶段，平面磨削在 M7130 平面磨床，外圆磨削在 M114W 外圆磨床；同一磨削工序内部依靠调整磨削深度与光磨次数实现多个表面不同粗糙度要求，避免拆分工序引入二次装夹误差。设置独立去毛刺工序置于抛光之前；调整表面处理顺序为先镀铬后刻字，防止镀层覆盖刻字纹路。抛光仅改善微观粗糙度，尺寸与形位精度全部由磨削工序保证。铣削工序对关键形位公差给出约束，整体适配大批大量生产条件。\
#indent 本工艺路线中，粗车工序分为两次装夹、分别加工左右两半部分：\

#grid(
  columns: 2,
  column-gutter: 4em,
  row-gutter: 1em,
  [工序1], [夹持右侧，粗车左半面],
  [工序2], [夹持左侧，粗车右半面],
)
=== 机械加工余量、工序尺寸及毛坯尺寸确定
#indent 法兰盘零件材料为 HT200，硬度 180HBS，弹性模量为 110GPa，稍逊于钢材。HT200 灰铸铁具有减震性好、耐磨性好、铸造及切削加工性能优良等特点，经人工时效消除内应力后，可辅以表面淬火或等离子体渗氮进一步提升局部耐磨性。\
#indent 依据《机械加工工艺手册》（以下简称《手册》），查得铸件加工余量、铸件尺寸公差；由零件最大外圆为$ph 100^(+0.34)_(-0.12)$可知，铸件机械加工余量公式为\
$ R=F+2"RMA"+"CT"/2 $
#grid(
  columns: 2,
  [式中：],
  [R#h(2em)铸件毛坯的基本尺寸\
    F#h(2em)最终机械加工后的尺寸\
    RMA#h(2em)要求的机械加工余量\
    CT#h(2em)铸件公差],
)
#indent 经《手册》表2-1查得，机器造型砂型铸造灰铸铁公差等级为 8 #sym.tilde 10 级；经《手册》表2-3查得，$ph 100$ 圆柱公差范围为 1.6 #sym.tilde 3.2mm；经《手册》表2-4查得，该构件要求的机械加工余量（RMA）为 0.5 #sym.tilde 1mm。将数据代入上式，得到\
$ R_max=100+2 times 1 + 3.2"/"2 =103.6 $
$ R_min=100+2 times 0.5 + 1.6/2=101.8 $
#indent 同理，铸件横向尺寸为 91mm，则：
$ R_max=91+2 times 1 + 3.2"/"2 =94.6 $
$ R_min=91+2 times 0.5 + 1.6/2=92.8 $

#figure(
  kind: table,
  caption: [加工余量表],
  table(
    align: center,
    column-gutter: 2em,
    row-gutter: 0.2em,
    stroke: none,
    columns: (auto, auto, auto, auto),
    table.hline(stroke: 1.2pt),
    table.cell(colspan: 2, align: center + horizon)[加工表面], [径向], [轴向],
    table.hline(stroke: 0.5pt),
    table.cell(rowspan: 2, align: center + horizon)[加工前尺寸], [最大], [103.6], [94.6],
    [最小], [101.8], [92.8],
    table.cell(rowspan: 2, align: center + horizon)[加工后尺寸], [最大], [100.34], [91],
    [最小], [99.88], [91],
    table.cell(rowspan: 2, align: center + horizon)[加工余量], [最大], [3.6], [3.6],
    [最小], [1.8], [1.8],
    table.cell(colspan: 2, align: center + horizon)[加工公差], [$frac(0.34, -0.12)$], [0],
    table.hline(stroke: 1.2pt),
  ),
)

#indent 为了保证加工的顺利进行、采购与铸造的便利以及坯料质检的容易，将坯料加工余量在最大值基础上略微向下取整，即坯料尺寸取 $ph 103 times 94 "mm"$。
== 工艺加工参数计算
=== 工序1基本参数
1.加工材料\
#indent 工件材料：HT200灰铸铁 180HBS\
#indent 加工要求：粗车$ph 100$柱体左侧端面、外圆及B面\
#indent 查询《机械制造技术基础课程设计指导教程》（以下简称《教程》）表4-1 卧式车床的型号与主要技术参数，经对比后选择卧式车床 CA6140。\
#indent 查询《手册》表4.2-1 车刀的前角及后角，可知加工灰铸铁应当选用高速钢车刀，且前角 $gamma_0 = 12 degree$，后角 $alpha_0 = 7 degree$，主偏角 $k_r = 75 degree$，刃倾角 $lambda_s = -5 degree$。\
#indent 由《教程》第五章“切削用量和时间定额的确定”第一节“切削用量的选择”查得，并结合本工序实际加工余量（单边约 1.8mm），取背吃刀量 $alpha_p = 2"mm"$。\
#indent 查询《手册》表2.4-3 硬质合金及高速钢车刀粗车外圆和端面时的进给量，加工直径 $ph 100$ 的铸铁工件时，车刀刀杆应选用 $"B" times "H" = 20 times 50 "mm"$，且在切削深度小于等于 3mm 的情况下进给量 $f = 1.0 #sym.tilde 1.4$，此处取中间值 $f = 1.2"mm/r"$。\
#indent 查询《手册》表2.4-1 车刀的磨损标准，外圆车刀粗车铸铁时后刀面最大磨损限度为 2.0mm。\
#indent 根据《手册》表2.4-10 车削时切削速度的计算公式，选用材料为 W18Cr4V 的高速钢刀具，其切削灰铸铁时的切削速度计算公式为
$ v = frac(c_v, T^(0.125) a_(p)^(0.15) f^(y_v)) k_v $
#grid(
  columns: 2,
  column-gutter: 1em,
  [式中：],
  [$v$#h(2em)切削速度\
   $T$#h(2em)刀具寿命\
    $a_p$#h(2em)背吃刀量\
    $f$#h(2em)进给量\
    $K_v$#h(2em)切削速度修正系数],
)
#indent 该表同时给出，当前条件下 $c_v = 22.7$，$y_v = 0.4$，刀具寿命取 $T = 60"min"$，切削速度修正系数 $K_v = 1.8$。将 $a_p = 2"mm"$、$f = 1.2"mm/r"$ 代入上式，得\
$ v = frac(22.7, 60^(0.125) times 2^(0.15) times 1.2^(0.4)) times 1.8 = 20.5"m/min" $\
#indent 由切削速度计算主轴转速\
$ n = frac(1000 v, pi d) = frac(1000 times 20.5, pi times 100) = 65.3"r/min" $\
#indent 根据 CA6140 卧式车床主轴转速表，选取与之相近的转速 $n = 63"r/min"$，则实际切削速度为\
$ v = frac(pi d n, 1000) = frac(pi times 100 times 63, 1000) = 19.8"m/min" $\
#indent 本工件为阶梯轴，各段尺寸依次为：$ph 100$ 圆盘宽 9mm，接 $ph 45$ 圆柱长 33mm，再接 $ph 90$ 圆盘宽 8mm，最后为 $ph 45$ 圆柱长 41mm，总长 91mm。由于各段直径不同，无法一次走刀完成，需按各段分别粗车。基本工时的计算公式为 $T_j = L/(f n)$，其中 $L$ 为刀具行程长度，$l_1 = 2"mm"$ 为切入量，$l_2 = 2"mm"$ 为切出量，进给量 $f = 1.2"mm/r"$，转速 $n = 63"r/min"$。\
==== 车 $ph 100$ 柱体左侧端面的基本工时
#indent 工件尚未钻孔，端面为实心，车刀由外圆走刀至中心，行程为 $L = d/2 + l_1 + l_2 = 50 + 2 + 2 = 54"mm"$，故其基本工时为\
$ T_j = frac(54, 1.2 times 63) = 0.71"min" $\
==== 车 $ph 100$ 外圆的基本工时
#indent $ph 100$ 外圆轴向长度为 9mm，行程为 $L = 9 + l_1 + l_2 = 9 + 2 + 2 = 13"mm"$，故其基本工时为\
$ T_j = frac(13, 1.2 times 63) = 0.17"min" $\
==== 车 B 面之 $ph 100$ 右端面的基本工时
#indent 车刀由 $ph 100$ 走刀至 $ph 45$，行程为 $L = (100-45)/2 + l_1 + l_2 = 27.5 + 2 + 2 = 31.5"mm"$，故其基本工时为\
$ T_j = frac(31.5, 1.2 times 63) = 0.42"min" $\
==== 车 B 面之 $ph 45$ 回转面的基本工时
#indent $ph 45$ 回转面轴向长度为 33mm，行程为 $L = 33 + l_1 + l_2 = 33 + 2 + 2 = 37"mm"$，故其基本工时为\
$ T_j = frac(37, 1.2 times 63) = 0.49"min" $\
==== 车 B 面之 $ph 90$ 左端面的基本工时
#indent 车刀由 $ph 90$ 走刀至 $ph 45$，行程为 $L = (90-45)/2 + l_1 + l_2 = 22.5 + 2 + 2 = 26.5"mm"$，故其基本工时为\
$ T_j = frac(26.5, 1.2 times 63) = 0.35"min" $\
#indent 故工序1的基本工时为各表面基本工时之和：\
$ T_j = 0.71 + 0.17 + 0.42 + 0.49 + 0.35 = 2.14"min" $
#indent 工序1的切削用量与工艺参数汇总如下：

#figure(
  kind: table,
  caption: [工序1工艺参数表],
  table(
    align: center,
    column-gutter: 1.5em,
    row-gutter: 0.2em,
    stroke: none,
    columns: (auto, auto, auto),
    table.hline(stroke: 1.2pt),

    table.cell(align: center + horizon)[类别], [参数项], [参数值],
    table.hline(stroke: 0.5pt),
    table.cell(rowspan: 5, align: center + horizon)[刀具],
    [刀具材料], [W18Cr4V 高速钢],
    [前角 $gamma_0$], [$12 degree$],
    [后角 $alpha_0$], [$7 degree$],
    [主偏角 $k_r$], [$75 degree$],
    [刃倾角 $lambda_s$], [$#sym.minus 5 degree$],
    table.cell(rowspan: 3, align: center + horizon)[切削参数],
    [背吃刀量 $a_p$], [$2"mm"$],
    [进给量 $f$], [$1.2"mm/r"$],
    [切削速度 $v$], [$ph 100"段" 19.8"m/min"$、$ph 90"段" 17.8"m/min"$、$ph 45"段" 8.9"m/min"$],
    table.cell(rowspan: 2, align: center + horizon)[机床],
    [所用机床], [CA6140 卧式车床],
    [主轴转速 $n$], [$63"r/min"$],
    table.cell(rowspan: 5, align: center + horizon)[基本工时 $T_j$],
    [车 $ph 100$ 左端面], [$0.71"min"$],
    [车 $ph 100$ 外圆], [$0.17"min"$],
    [车 B 面之 $ph 100$ 右端面], [$0.42"min"$],
    [车 B 面之 $ph 45$ 回转面], [$0.49"min"$],
    [车 B 面之 $ph 90$ 左端面], [$0.35"min"$],
    [合计总工时], [],[$2.14"min"$],
    table.hline(stroke: 1.2pt),
  ),
)

=== 工序2基本参数
1.加工材料\
#indent 工件材料：HT200灰铸铁 180HBS\
#indent 加工要求：粗车$ph 45$右端面、$ph 45$外圆、$ph 90$外圆及$ph 90$右端面\
#indent 本工序所用机床、刀具及背吃刀量、进给量等切削用量均与工序1相同，即选用 CA6140 卧式车床，采用 W18Cr4V 高速钢车刀，背吃刀量 $alpha_p = 2"mm"$，进给量 $f = 1.2"mm/r"$。\
#indent 本工序最大加工直径为 $ph 90$，其切削速度计算公式及系数与工序1相同，故理论切削速度仍为\
$ v = frac(22.7, 60^(0.125) times 2^(0.15) times 1.2^(0.4)) times 1.8 = 20.5"m/min" $\
#indent 由切削速度按最大直径 $ph 90$ 计算主轴转速\
$ n = frac(1000 v, pi d) = frac(1000 times 20.5, pi times 90) = 72.6"r/min" $\
#indent 根据 CA6140 卧式车床主轴转速表，选取与之相近的转速 $n = 63"r/min"$，故 $ph 90$ 段实际切削速度为\
$ v = frac(pi d n, 1000) = frac(pi times 90 times 63, 1000) = 17.8"m/min" $\
#indent 各段实际切削速度随直径不同，$ph 90$ 段为 17.8 m/min，$ph 45$ 段为 8.9 m/min。基本工时的计算公式为 $T_j = L/(f n)$，其中 $L$ 为刀具行程长度，$l_1 = 2"mm"$ 为切入量，$l_2 = 2"mm"$ 为切出量，进给量 $f = 1.2"mm/r"$，转速 $n = 63"r/min"$。\
==== 车 $ph 45$ 右端面的基本工时
#indent 工件尚未钻孔，端面为实心，车刀由外圆走刀至中心，行程为 $L = d/2 + l_1 + l_2 = 22.5 + 2 + 2 = 26.5"mm"$，故其基本工时为\
$ T_j = frac(26.5, 1.2 times 63) = 0.35"min" $\
==== 车 $ph 45$ 外圆的基本工时
#indent $ph 45$ 外圆轴向长度为 41mm，行程为 $L = 41 + l_1 + l_2 = 41 + 2 + 2 = 45"mm"$，故其基本工时为\
$ T_j = frac(45, 1.2 times 63) = 0.60"min" $\
==== 车 $ph 90$ 右端面的基本工时
#indent 车刀由 $ph 90$ 走刀至 $ph 45$，行程为 $L = (90-45)/2 + l_1 + l_2 = 22.5 + 2 + 2 = 26.5"mm"$，故其基本工时为\
$ T_j = frac(26.5, 1.2 times 63) = 0.35"min" $\
==== 车 $ph 90$ 外圆的基本工时
#indent $ph 90$ 圆盘轴向宽度为 8mm，行程为 $L = 8 + l_1 + l_2 = 8 + 2 + 2 = 12"mm"$，故其基本工时为\
$ T_j = frac(12, 1.2 times 63) = 0.16"min" $\
#indent 故工序2的基本工时为各表面基本工时之和：\
$ T_j = 0.35 + 0.60 + 0.35 + 0.16 = 1.46"min" $
#indent 工序2的切削用量与工艺参数汇总如下：

#figure(
  kind: table,
  caption: [工序2 工艺参数表],
  table(
    align: center,
    column-gutter: 1.5em,
    row-gutter: 0.2em,
    stroke: none,
    columns: (auto, auto, auto),
    table.hline(stroke: 1.2pt),
    table.cell(colspan: 3, align: center + horizon)[工序2　粗车 $ph 45$ 右端面、外圆及 $ph 90$ 外圆及其右端面],
    table.hline(stroke: 0.5pt),
    table.cell(align: center + horizon)[类别], [参数项], [参数值],
    table.hline(stroke: 0.5pt),
    table.cell(rowspan: 5, align: center + horizon)[刀具],
    [刀具材料], [W18Cr4V 高速钢],
    [前角 $gamma_0$], [$12 degree$],
    [后角 $alpha_0$], [$7 degree$],
    [主偏角 $k_r$], [$75 degree$],
    [刃倾角 $lambda_s$], [$#sym.minus 5 degree$],
    table.cell(rowspan: 3, align: center + horizon)[切削参数],
    [背吃刀量 $a_p$], [$2"mm"$],
    [进给量 $f$], [$1.2"mm/r"$],
    [切削速度 $v$], [$ph 90"段" 17.8"m/min"$、$ph 45"段" 8.9"m/min"$],
    table.cell(rowspan: 2, align: center + horizon)[机床],
    [所用机床], [CA6140 卧式车床],
    [主轴转速 $n$], [$63"r/min"$],
    table.cell(rowspan: 4, align: center + horizon)[基本工时 $T_j$],
    [车 $ph 45$ 右端面], [$0.35"min"$],
    [车 $ph 45$ 外圆], [$0.60"min"$],
    [车 $ph 90$ 右端面], [$0.35"min"$],
    [车 $ph 90$ 外圆], [$0.16"min"$],
    table.hline(stroke: 0.5pt),
    [合计总工时], [],[$1.46"min"$],
    table.hline(stroke: 1.2pt),
  ),
)

=== 工序3基本参数
1.加工材料\
#indent 工件材料：HT200灰铸铁 180HBS\
#indent 加工要求：半精车$ph 100$回转面、左端面及 B 面，并在 B 面两侧圆柱靠近 B 面的边线上倒 C1.5 角\
#indent 半精车采用硬质合金 YG6 车刀，前角 $gamma_0 = 6 degree$，主后角 $alpha_0 = 7 degree$，刃倾角 $lambda_s = +3 degree$，副偏角 $k_r^' = 6 degree$，背吃刀量 $alpha_p = 1"mm"$，进给量 $f = 0.4"mm/r"$，加工设备仍选用 CA6140 卧式车床。\
#indent 本工序各表面的粗糙度要求为：$ph 100$ 右端面 Ra1.6，$ph 100$ 回转面 Ra0.8，B 面 Ra0.4，$ph 90$ 回转面 Ra6.4。半精车为后续精车与磨削留有加工余量，最终由精加工保证上述粗糙度。\
#indent 硬质合金车刀车削灰铸铁的切削速度计算公式为\
$ v = frac(c_v, T^(0.2) a_(p)^(0.15) f^(0.2)) k_v $
#grid(
  columns: 2,
  column-gutter: 1em,
  [式中：],
  [$v$#h(2em)切削速度\
   $T$#h(2em)刀具寿命\
    $a_p$#h(2em)背吃刀量\
    $f$#h(2em)进给量\
    $K_v$#h(2em)切削速度修正系数],
)
#indent 经查《手册》，硬质合金车削灰铸铁时 $c_v = 71$，刀具寿命取 $T = 60"min"$，修正系数 $K_v = 1$。将 $a_p = 1"mm"$、$f = 0.4"mm/r"$ 代入上式，得\
$ v = frac(71, 60^(0.2) times 1^(0.15) times 0.4^(0.2)) = 37.6"m/min" $\
#indent 本工序最大加工直径为 $ph 100$，由切削速度计算主轴转速\
$ n = frac(1000 v, pi d) = frac(1000 times 37.6, pi times 100) = 119.7"r/min" $\
#indent 根据 CA6140 卧式车床主轴转速表，选取与之相近的转速 $n = 125"r/min"$，故 $ph 100$ 段实际切削速度为\
$ v = frac(pi d n, 1000) = frac(pi times 100 times 125, 1000) = 39.3"m/min" $\
#indent 各段实际切削速度随直径不同，$ph 100$ 段为 39.3 m/min，$ph 90$ 段为 35.3 m/min，$ph 45$ 段为 17.7 m/min。基本工时的计算公式为 $T_j = L/(f n)$，其中 $L$ 为刀具行程长度，$l_1 = 2"mm"$ 为切入量，$l_2 = 2"mm"$ 为切出量，进给量 $f = 0.4"mm/r"$，转速 $n = 125"r/min"$。\
==== 半精车 $ph 100$ 回转面的基本工时
#indent $ph 100$ 回转面轴向长度为 9mm，行程为 $L = 9 + l_1 + l_2 = 9 + 2 + 2 = 13"mm"$，故其基本工时为\
$ T_j = frac(13, 0.4 times 125) = 0.26"min" $\
==== 半精车 $ph 100$ 左端面的基本工时
#indent 工件左端面为实心，车刀由外圆走刀至中心，行程为 $L = d/2 + l_1 + l_2 = 50 + 2 + 2 = 54"mm"$，故其基本工时为\
$ T_j = frac(54, 0.4 times 125) = 1.08"min" $\
==== 半精车 B 面之 $ph 100$ 右端面的基本工时
#indent 车刀由 $ph 100$ 走刀至 $ph 45$，行程为 $L = (100-45)/2 + l_1 + l_2 = 27.5 + 2 + 2 = 31.5"mm"$，故其基本工时为\
$ T_j = frac(31.5, 0.4 times 125) = 0.63"min" $\
==== 半精车 B 面之 $ph 45$ 回转面的基本工时
#indent $ph 45$ 回转面轴向长度为 33mm，行程为 $L = 33 + l_1 + l_2 = 33 + 2 + 2 = 37"mm"$，故其基本工时为\
$ T_j = frac(37, 0.4 times 125) = 0.74"min" $\
==== 半精车 B 面之 $ph 90$ 左端面的基本工时
#indent 车刀由 $ph 90$ 走刀至 $ph 45$，行程为 $L = (90-45)/2 + l_1 + l_2 = 22.5 + 2 + 2 = 26.5"mm"$，故其基本工时为\
$ T_j = frac(26.5, 0.4 times 125) = 0.53"min" $\
==== 倒 C1.5 角的基本工时
#indent B 面两侧圆柱靠近 B 面的边线需倒 C1.5 角，倒角行程取 $L = 3"mm"$，故其基本工时为\
$ T_j = frac(3, 0.4 times 125) = 0.06"min" $\
#indent 故工序3的基本工时为各表面基本工时之和：\
$ T_j = 0.26 + 1.08 + 0.63 + 0.74 + 0.53 + 0.06 = 3.30"min" $
#indent 工序3的切削用量与工艺参数汇总如下：

#figure(
  kind: table,
  caption: [工序3 工艺参数表],
  table(
    align: center,
    column-gutter: 1.5em,
    row-gutter: 0.2em,
    stroke: none,
    columns: (auto, auto, auto),
    table.hline(stroke: 1.2pt),
    table.cell(colspan: 3, align: center + horizon)[工序3　半精车 $ph 100$ 回转面、左端面及 B 面，并倒 C1.5 角],
    table.hline(stroke: 0.5pt),
    table.cell(align: center + horizon)[类别], [参数项], [参数值],
    table.hline(stroke: 0.5pt),
    table.cell(rowspan: 5, align: center + horizon)[刀具],
    [刀具材料], [硬质合金 YG6],
    [前角 $gamma_0$], [$6 degree$],
    [主后角 $alpha_0$], [$7 degree$],
    [刃倾角 $lambda_s$], [$+3 degree$],
    [副偏角 $k_r^'$], [$6 degree$],
    table.cell(rowspan: 3, align: center + horizon)[切削参数],
    [背吃刀量 $a_p$], [$1"mm"$],
    [进给量 $f$], [$0.4"mm/r"$],
    [切削速度 $v$], [$ph 100"段" 39.3"m/min"$、$ph 90"段" 35.3"m/min"$、$ph 45"段" 17.7"m/min"$],
    table.cell(rowspan: 2, align: center + horizon)[机床],
    [所用机床], [CA6140 卧式车床],
    [主轴转速 $n$], [$125"r/min"$],
    table.cell(rowspan: 6, align: center + horizon)[基本工时 $T_j$],
    [半精车 $ph 100$ 回转面], [$0.26"min"$],
    [半精车 $ph 100$ 左端面], [$1.08"min"$],
    [半精车 B 面之 $ph 100$ 右端面], [$0.63"min"$],
    [半精车 B 面之 $ph 45$ 回转面], [$0.74"min"$],
    [半精车 B 面之 $ph 90$ 左端面], [$0.53"min"$],
    [倒 C1.5 角], [$0.06"min"$],
    table.hline(stroke: 0.5pt),
    [合计总工时], [],[$3.30"min"$],
    table.hline(stroke: 1.2pt),
  ),
)

=== 工序4基本参数
1.加工材料\
#indent 工件材料：HT200灰铸铁 180HBS\
#indent 加工要求：半精车$ph 45$外圆及右端面，倒 C7 大倒角，半精车$ph 90$右端面，并切$ph 45$与$ph 90$之间的 3×2 退刀槽\
#indent 本工序所用机床、刀具及切削用量均与工序3相同，即选用 CA6140 卧式车床，采用硬质合金 YG6 车刀，背吃刀量 $alpha_p = 1"mm"$，进给量 $f = 0.4"mm/r"$。\
#indent 本工序最大加工直径为 $ph 90$，切削速度计算与工序3相同，理论切削速度为 $37.6"m/min"$。由切削速度按最大直径 $ph 90$ 计算主轴转速\
$ n = frac(1000 v, pi d) = frac(1000 times 37.6, pi times 90) = 133.0"r/min" $\
#indent 根据 CA6140 卧式车床主轴转速表，选取与之相近的转速 $n = 125"r/min"$，故 $ph 90$ 段实际切削速度为\
$ v = frac(pi d n, 1000) = frac(pi times 90 times 125, 1000) = 35.3"m/min" $\
#indent 各段实际切削速度随直径不同，$ph 90$ 段为 35.3 m/min，$ph 45$ 段为 17.7 m/min。基本工时的计算公式为 $T_j = L/(f n)$，其中 $L$ 为刀具行程长度，$l_1 = 2"mm"$ 为切入量，$l_2 = 2"mm"$ 为切出量，进给量 $f = 0.4"mm/r"$，转速 $n = 125"r/min"$。\
==== 半精车 $ph 45$ 外圆的基本工时
#indent $ph 45$ 外圆轴向长度为 41mm，行程为 $L = 41 + l_1 + l_2 = 41 + 2 + 2 = 45"mm"$，故其基本工时为\
$ T_j = frac(45, 0.4 times 125) = 0.90"min" $\
==== 半精车 $ph 45$ 右端面的基本工时
#indent 工件右端面为实心，车刀由外圆走刀至中心，行程为 $L = d/2 + l_1 + l_2 = 22.5 + 2 + 2 = 26.5"mm"$，故其基本工时为\
$ T_j = frac(26.5, 0.4 times 125) = 0.53"min" $\
==== 倒 C7 大倒角的基本工时
#indent 工件最右端为 C7 大倒角，为 45 度倒角，倒角行程取 $L = 10"mm"$，故其基本工时为\
$ T_j = frac(10, 0.4 times 125) = 0.20"min" $\
==== 半精车 $ph 90$ 右端面的基本工时
#indent 车刀由 $ph 90$ 走刀至 $ph 45$，行程为 $L = (90-45)/2 + l_1 + l_2 = 22.5 + 2 + 2 = 26.5"mm"$，故其基本工时为\
$ T_j = frac(26.5, 0.4 times 125) = 0.53"min" $\
==== 切 3×2 退刀槽的基本工时
#indent $ph 45$ 与 $ph 90$ 之间的退刀槽为 3×2，切槽行程取 $L = 3"mm"$，故其基本工时为\
$ T_j = frac(3, 0.4 times 125) = 0.06"min" $\
#indent 故工序4的基本工时为各表面基本工时之和：\
$ T_j = 0.90 + 0.53 + 0.20 + 0.53 + 0.06 = 2.22"min" $
#indent 工序4的切削用量与工艺参数汇总如下：

#figure(
  kind: table,
  caption: [工序4 工艺参数表],
  table(
    align: center,
    column-gutter: 1.5em,
    row-gutter: 0.2em,
    stroke: none,
    columns: (auto, auto, auto),
    table.hline(stroke: 1.2pt),
    table.cell(colspan: 3, align: center + horizon)[工序4　半精车 $ph 45$ 外圆、右端面及 $ph 90$ 右端面，倒 C7 角，切 3×2 退刀槽],
    table.hline(stroke: 0.5pt),
    table.cell(align: center + horizon)[类别], [参数项], [参数值],
    table.hline(stroke: 0.5pt),
    table.cell(rowspan: 5, align: center + horizon)[刀具],
    [刀具材料], [硬质合金 YG6],
    [前角 $gamma_0$], [$6 degree$],
    [主后角 $alpha_0$], [$7 degree$],
    [刃倾角 $lambda_s$], [$+3 degree$],
    [副偏角 $k_r^'$], [$6 degree$],
    table.cell(rowspan: 3, align: center + horizon)[切削参数],
    [背吃刀量 $a_p$], [$1"mm"$],
    [进给量 $f$], [$0.4"mm/r"$],
    [切削速度 $v$], [$ph 90"段" 35.3"m/min"$、$ph 45"段" 17.7"m/min"$],
    table.cell(rowspan: 2, align: center + horizon)[机床],
    [所用机床], [CA6140 卧式车床],
    [主轴转速 $n$], [$125"r/min"$],
    table.cell(rowspan: 5, align: center + horizon)[基本工时 $T_j$],
    [半精车 $ph 45$ 外圆], [$0.90"min"$],
    [半精车 $ph 45$ 右端面], [$0.53"min"$],
    [倒 C7 大倒角], [$0.20"min"$],
    [半精车 $ph 90$ 右端面], [$0.53"min"$],
    [切 3×2 退刀槽], [$0.06"min"$],
    table.hline(stroke: 0.5pt),
    [合计总工时], [],[$2.22"min"$],
    table.hline(stroke: 1.2pt),
  ),
)





